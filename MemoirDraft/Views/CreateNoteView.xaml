<Window x:Class="MemoirDraft.Views.CreateNoteView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CreateNote" 
        Height="600" Width="600"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}"
        WindowStyle="None"
        ResizeMode="NoResize">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border x:Name="HeaderBorder" Grid.Row="0" 
                Background="{StaticResource AccentRedBrush}" 
                BorderBrush="{StaticResource GoldBrush}" 
                BorderThickness="0,0,0,2"
                MouseLeftButtonDown="HeaderBorder_MouseLeftButtonDown">
            <Grid Margin="15,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="⚙️" FontSize="20" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBlock Text="СОЗДАНИЕ ЗАМЕТКИ" 
                               Foreground="{StaticResource TextPrimaryBrush}" 
                               FontWeight="Bold" FontSize="18"
                               VerticalAlignment="Center"/>
                </StackPanel>

                <Button Grid.Column="2" 
                        Content="✕" 
                        Width="30" Height="30" 
                        Background="#660000" 
                        BorderBrush="{StaticResource GoldBrush}" 
                        BorderThickness="1"
                        Foreground="{StaticResource TextPrimaryBrush}"
                        FontSize="16" 
                        Cursor="Hand"
                        Command="{Binding CloseCommand}"
                        VerticalAlignment="Center" 
                        HorizontalAlignment="Right"/>
            </Grid>
        </Border>

        <!-- Панель переключения типов (вкладки) -->
        <Border Grid.Row="1" Background="{StaticResource PanelBrush}" 
                BorderBrush="{StaticResource GoldBrush}" BorderThickness="0,0,0,1">
            <ItemsControl ItemsSource="{Binding Pages}" Margin="20,10">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Content="{Binding Name}" 
                                Margin="0,0,20,0"
                                Command="{Binding DataContext.SwitchPageCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                CommandParameter="{Binding}"
                                Cursor="Hand">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource TabButton}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsActive}" Value="True">
                                            <Setter Property="Foreground" Value="{StaticResource GoldBrush}"/>
                                            <Setter Property="BorderBrush" Value="{StaticResource GoldBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Border>

        <!-- Область страниц -->
        <ContentControl Grid.Row="2" 
                Content="{Binding ActivePage.Content}"
                Background="{StaticResource BackgroundBrush}"
                Margin="20"/>
    </Grid>
</Window>